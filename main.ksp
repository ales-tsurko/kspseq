import "ViewController.ksp"

{ 
  приватные функции обозначены двумя подчеркиваниями: __func_name()
  они предназначены для внутренней работы. Соответственно публичными функциями
  являются только те, которые не имеют такого подчеркивания перед именем. 
  Публичные функции предназначены для взаимодействия с API.
}

on init
  init_view_controller()
end on
